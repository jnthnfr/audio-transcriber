# Frontend env. Vite exposes vars prefixed with VITE_ to client code.
# Local dev leaves this blank — the Vite proxy forwards /api to localhost:8000.
# In production, set this to your deployed backend URL.

VITE_API_BASE_URL=
